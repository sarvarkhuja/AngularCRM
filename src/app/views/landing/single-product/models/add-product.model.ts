export interface AddProductModel {
  firstName: string;
  city: string;
  district: string;
  productVersionId: number;
  quantity: number;
  promoCode: string;
  phoneNumber: string;
  note?: string;
  productId?: number;
}
